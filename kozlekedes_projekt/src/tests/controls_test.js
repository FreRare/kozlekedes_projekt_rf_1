//ez mar a szarabbik melo, itt kell megirni a teszteket :(

const Controls=require("../backend/CONTROLS/Controls");
controlsObject= new Controls();

//loginByEmailAndPassword tesztelese
/*QUnit.module('loginByEmailAndPassword', function() {
    QUnit.test('email and password', function(assert) {
        assert.equal(controlsObject.loginByEmailAndPassword("admin@admin", "admin"), false);
    });
});*/

/*test("LoginByEmailAndPassword", function(){
    equal(controlsObject.loginByEmailAndPassword("admin@admin", "admin"), false);
});*/

//egyik se jo :(